fn ex_stack_heap() {
    /*
    fn main() {
        // Heap
        let nome: String = String::from("Rust");
        // Stack
        let idade: i32 = 32;

        println!("Nome -> {}", nome);
        // println!("Idade -> {}", idade);
        exibir_idade(&idade);
    }

    // Stack
    fn exibir_idade(idade: &i32) {
        println!("Idade -> {}", idade);
    }
    */
}
